{
  "name": "ih26-workshop",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "node scripts/generate-papers-metadata.js && vite",
    "build": "node scripts/generate-papers-metadata.js && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^5.0.8"
  }
}
